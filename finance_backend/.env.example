# Database Configuration
# Opção 1: URL completa do banco de dados
DATABASE_URL=postgresql://user:password@localhost:5432/finance_db

# Opção 2: Parâmetros individuais do banco de dados
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=finance_db
# DB_USER=user
# DB_PASSWORD=password

# JWT Configuration
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# App Configuration
APP_NAME=Finances API
DEBUG=True

# Celery Configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Email Configuration (SMTP)
# Para Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu-email@gmail.com
# SMTP_PASSWORD=sua-senha-app-specific

# Para SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=SG.xxxxxxxxxxxxxxxxxxxxx

# Para Mailgun:
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=postmaster@seudominio.com
# SMTP_PASSWORD=sua-senha-mailgun

# Configuração básica (exemplo genérico)
SMTP_HOST=smtp.seudominio.com
SMTP_PORT=587
SMTP_USER=seu-email@seudominio.com
SMTP_PASSWORD=sua-senha-smtp
EMAILS_FROM_EMAIL=no-reply@seuapp.com
EMAILS_FROM_NAME=Finances API Alertas
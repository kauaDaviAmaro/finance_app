# ============================================================================
# FINANCE APP - ENVIRONMENT VARIABLES EXAMPLE
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to Git!

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=finances_db
DB_USER=postgres
DB_PASSWORD=postgres

# Alternative: Use a full database URL instead of individual parameters
# DATABASE_URL=postgresql://user:password@host:port/dbname

# ============================================================================
# JWT SECURITY
# ============================================================================
SECRET_KEY=your-secret-key-here-change-in-production
# Algorithm is optional, defaults to HS256
# ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================================================
# CELERY CONFIGURATION
# ============================================================================
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
CELERY_WORKER_CONCURRENCY=1

# ============================================================================
# EMAIL CONFIGURATION (Optional)
# ============================================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAILS_FROM_EMAIL=no-reply@seuapp.com
EMAILS_FROM_NAME=Finances API Alertas

# ============================================================================
# STRIPE CONFIGURATION
# ============================================================================
# Get these from: https://dashboard.stripe.com/test/apikeys (for test mode)
# See STRIPE_SETUP.md for detailed instructions

# Stripe Secret Key (starts with sk_test_ for test mode, sk_live_ for production)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxx

# Stripe Price ID for PRO subscription (starts with price_)
# Create a product and price in Stripe Dashboard, then copy the Price ID
STRIPE_PRICE_ID_PRO=price_xxxxxxxxxxxxxxxxxxxxx

# Stripe Webhook Secret (starts with whsec_)
# Get this from Stripe Dashboard → Developers → Webhooks → Your webhook → Signing secret
# Or use Stripe CLI: stripe listen --forward-to localhost:8000/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxx

# URLs for Stripe redirects (adjust ports/domains as needed)
STRIPE_SUCCESS_URL=http://localhost:3000/subscription/success
STRIPE_CANCEL_URL=http://localhost:3000/subscription/cancel
STRIPE_RETURN_URL=http://localhost:3000/subscription

# ============================================================================
# FRONTEND CONFIGURATION (Optional)
# ============================================================================
# VITE_API_BASE_URL=http://localhost:8000

# ============================================================================
# PORT CONFIGURATION (Optional - defaults shown)
# ============================================================================
# API_PORT=8000
# FRONTEND_PORT=5173
# REDIS_PORT=6379
